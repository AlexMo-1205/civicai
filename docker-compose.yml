services:
  civicai:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Persiste la base vectorielle entre les redémarrages
      - ./chroma_db:/app/chroma_db
